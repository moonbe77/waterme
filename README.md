### Generate migrations <https://orm.drizzle.team/docs/get-started-sqlite#expo-sqlite>

- After creating SQL schema file and drizzle.config.ts file, you can generate migrations
  `npx drizzle-kit generate:sqlite`
